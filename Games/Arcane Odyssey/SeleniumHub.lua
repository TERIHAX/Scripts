loadstring(game:HttpGet('https://githubusercontent.com/synnyyy/synergy/raw/main/selenium.lua', true))({
	QueueOnTeleport = false -- This executes Selenium on server teleportation. If you want to enable this, set this to true. Otherwise set it to false.
})

-- Supported games are as follows:

-- Rate my Avatar
-- Pixel Piece
-- Pilgrammed
-- Arcane Odyssey
-- My Restaurant

---------------

--[[
    To get key, you gotta go in the discord, verify in the verification channel, then authenticate in that same channel. then type /key in the commands channel.
]]--
